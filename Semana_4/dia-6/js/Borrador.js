// objetos
// *En el mundo real
    // Un elemento que ocupa un espacio;
    // Tiene atributos claramente identificables;
    // Tiene caracteristicas (atributos / funcionalidades);
    // Tiene un estado y un comportamiento
        //Estado: el conjunto de atributos
        // Comportamientos: el conjunto de funciones
    // Ejemplos
    // -carro:
    //       estado(marca, motor, color, año, ancho, largo, modelo)
    //       comportamiento(encender, avanzar, girar, frenar, abrir_puertas)
    
    // -persona
    //      estado(altura, peso, color, edad, religion,iq, ojos_color, ojos_cantidad)
    //          -mano_tamaño
    //          -mano_cantidad
    //          -mano: 
    //              -estado(dedos_cantidad, color, tamaño)
    //              -comportamiento(agarrar, golpear, puño, dibujar, codificar)
    // -laptop

    // -casa
    // -pensamiento
//* a nivel de informacion
    // -carro
    // -persona
    // -sentimiento
    //      estado(origen, animo, intensidad)
            // comportamiento(reaccion, eliminacion)
    // -pensamiento


// abstraccion
    // *Capacidad mental para identificar aquellos atributos y funciones de objetos


    // *Paradigma(estilo, modelo) orientado a objetos
    // *Los lenguajes te permiten crear objetos (
    // *    definir y dar valor a sus atributos / programar metodos que arrojan 
    // *    resultados
    // *)

    // !Blueprint (es plano / molde)
    
        // 1.  crear la clase (molde / plano /blueprint)
        // 2.  definir atributos o propiedades
        // 3.  definir sus metodos y funciones
        // 4.  metodo constructor()
        // -------------------------------------
        // 5.  Instanciar el objeto (new)
        // 5.1     Darle valor a sus atributos
        // 6.  Utilizar al objeto a traves de sus metodos

        // Ejemplo

        // a partir de la class carro puedo crear 
        //     -   carro rojo /    velocidad maxima = 200; / vivio desde hoy  / murio 2024
        //     -   carro azul  /   velocidad maxima = 50; / vivio desde hoy / -----
        //     -   carro verde /   velocidad maxima = 80;