// while (condicion){}

// nota: un while suele ir acompañado de un contador
let contador = 0;

while( contador < 9){
    console.log(contador);// 0
    //para que mi condicion no sea siempre true
    // vamos  a sumarlo a contador 1 en cada vuelta
    // contador = contador + 1
    // contador = 0 + 1
    // contador = 1
    contador += 1;
}

